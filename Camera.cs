using System;
using OpenTK;
namespace Template
{
    public class Camera
    {
        public Vector3 postion;

        public Camera(Vector3 pos)
        {
            postion = pos;
        }
    }
}
